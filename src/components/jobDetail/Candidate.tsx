import { Card, CardContent } from '../ui/card';

const Candidate = () => {
    return (
        <Card className="w-full border bg-white/50 pt-5">
            <CardContent className="pb-5">Candidate</CardContent>
        </Card>
    );
};

export default Candidate;
